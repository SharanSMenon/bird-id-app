import React from "react";

export default function Header() {
    return (
        <header className="header">
            <h1 className="display-1">Bird Identifier (BETA)</h1>
        </header>
    );
}